# ansible-labs
Starter files for BOAQAXIP Ansible labs

## Prerequisites
* A running QwikLabs session with cloudshell and the cloud editor open
* This repository cloned into the lab environment:
```bash
git clone --recurse-submodules https://github.com/qa-tech-training/ansible-labs.git
```